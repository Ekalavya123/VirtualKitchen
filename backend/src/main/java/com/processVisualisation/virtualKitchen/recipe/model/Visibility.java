package com.processVisualisation.virtualKitchen.recipe.model;


/**
 * Controls who can see and use a {@link RecipeTemplate}: visible to everyone
 * ({@code PUBLIC}) or only to its creator ({@code PRIVATE}).
 */
public enum

Visibility {
    PUBLIC,
    PRIVATE
}
